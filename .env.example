# Optional: URL of your deployed Cloudflare Worker (worker/). With it, people who enter an
# access code get Google photos, ratings and prices. Without it: free OpenStreetMap data.
# The Google key itself never goes here — it's a secret on the Worker.
VITE_PLACES_PROXY_URL=
